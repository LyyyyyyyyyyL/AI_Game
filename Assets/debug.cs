using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class debug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("GameWinScene has started!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
